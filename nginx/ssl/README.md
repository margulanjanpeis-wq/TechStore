# SSL Certificates

SSL certificates are generated automatically when the stack starts via the `ssl-generator` service in docker-compose.yml.

For production, replace these with real certificates from Let's Encrypt or your CA.
